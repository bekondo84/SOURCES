<nav class="navbar navbar-default navbar-custom navbar-fixed-top">
	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header page-scroll">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
				<span class="sr-only">Toggle navigation</span>
				Menu <i class="fa fa-bars"></i>
			</button>
			<a ng-if="currentuser.username=='website@website'" class="navbar-brand" href="#/website/portailweb" ng-click="goto('about')">Start Bootstrap</a>
			<a ng-if="currentuser.username!='website@website'" class="navbar-brand" href="#" ng-click="gotoModulesMenu()">
				 <span class="glyphicon glyphicon-th"  aria-hidden="true" ></span>  
			</a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav navbar-right">
				<li>
					<a href="#" ng-click="goto('index')">Home</a>
				</li>
				<li>
					<a href="#" ng-click="goto('about')">About</a>
				</li>
				<li>
					<a href="#" ng-click="goto('post')">Sample Post</a>
				</li>
				<li>
					<a href="#" ng-click="goto('contact')">Contact</a>
				</li>
			</ul>
		</div>
		<!-- /.navbar-collapse -->
	</div>
	<!-- /.container -->
</nav>